# Work Log — 2026-06-27

## Goal activated
- Created active goal: make littleman application-agnostic, self-editing, and continue toward MVP using existing plans.
- Audited Polymarket references across core platform modules (`agent/session.py`, `api/routes/agent.py`, `config.py`, `db/models.py`, `llm/client.py`, `llm/prompts.py`, `macro/goal_tree.py`, `skills/registry.py`, `tasks/executor.py`).

## Repo hygiene
- Added `.gitattributes` to normalize text files to LF and committed line-ending normalization.
- Updated stale test counts: `README.md` (65 → 102), `ROADMAP.md` (94 → 102).
- Updated `docs/META.md` construct status table: CALENDAR.md and EXPOSURE.md are now **built**.

## Application decoupling
- Added `Application` protocol + built-in registry in `littleman/applications/`.
- Moved Polymarket skills/client to `littleman/applications/polymarket/` with `PolymarketApplication`.
- Made skill registry, session reconcile, task EXECUTE, dashboard status, and goal root dynamic per active application.
- Genericized directive/strategy/heartbeat/first-light prompts via `agent_description()`.
- Removed hard-coded Polymarket tool definitions from `llm/client.py`.
- Renamed `Position.polymarket_order_id` → `external_order_id`.
- Changed default `active_application` to `littleman.platform`.
- Committed: `cf02bec` (decoupling) + `48f2bf2` (worklog).

## Workspace self-editing surface
- Added `littleman/skills/workspace_files.py` with `list/read/write/update_workspace_file` skills.
- Registered skills in `build_registry()`.
- Added per-turn workspace markdown discovery in `meta/construct.py` and injected discovered files into the directive prompt.
- Added tests: `test_workspace_files.py` (7), `test_workspace_discovery.py` (2).
- Suite: **111 tests passing**.

## Next
- Implement `update_self` skill for custom onboarding.
- Add `TURNS.md` turn-cycle execution window.
- Add OpenClaw `SKILL.md` filesystem loader.
